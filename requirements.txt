# Core requirements for TaxaMask / Formica-Flow.
#
# This file intentionally keeps PyTorch generic. For a fresh environment, install
# one of the CPU/CUDA PyTorch variants first, then install this file:
#   pip install -r requirements-torch-cpu.txt
#   pip install -r requirements.txt
# or
#   pip install -r requirements-torch-cu121.txt
#   pip install -r requirements.txt

# --- Core GUI & Logic ---
PySide6>=6.0.0
numpy>=1.26.0
pandas>=2.1.0

# --- Image Processing & CV ---
opencv-python>=4.5.0
Pillow>=10.2.0
matplotlib>=3.5.0
pdf2image>=1.17.0

# --- PDF Processing ---
pdfplumber>=0.10.3
PyMuPDF>=1.23.0

# --- AI & Machine Learning ---
# Install torch/torchvision from requirements-torch-cpu.txt or
# requirements-torch-cu121.txt before this file so the hardware-specific
# PyTorch build is not replaced by a generic wheel.
ultralytics>=8.2.0
easyocr>=1.7.1

# --- LLM & API ---
openai>=1.12.0
anthropic>=0.18.0
requests>=2.31.0
