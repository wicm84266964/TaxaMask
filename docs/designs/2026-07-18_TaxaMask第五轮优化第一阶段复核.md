# TaxaMask 第五轮优化第一阶段复核

- 日期：2026-07-18
- 阶段：M0 锁定现状与安全边界
- 结论：通过，可以进入 M1

## 已完成

1. 盘清 10 个稳定训练入口、8 个统一记录边界，并记录输入、参数、输出、权重、报告和备注现状。
2. 封住 headless 二维训练读取未复核 Auto-Annotated polygon 的旁路。
3. 移除 Local Axis 的 `include_unconfirmed` 入口，核心层也固定拒绝绕过请求。
4. 保留“通过所选 AI 结果”，只提升用户明确选择项，并把白名单审核信息持久化到 `manual_truth.review_audit`。
5. 建立 5 个推理组合场景的冻结基线，覆盖成功、跳过、专家采用/拒绝和 SAM 失败。
6. 冻结完整性、实际拆分、训练、推理和网格导出契约；项目 SQLite 是网格导出的唯一状态、对账和恢复来源，不生成默认网格 JSON sidecar。

## 验证证据

- 第一阶段回归：311/311 通过，使用 `taxamask` 环境的 `unittest`。
- 契约示例：8/8 个 fenced JSON 可解析。
- 推理基线：schema 正确，5 个场景，已强制纳入 Git 跟踪。
- 安全检查：新增内容未发现密钥、用户名或私人绝对路径；`git diff --check` 通过。
- 独立复核：初审发现的建档时点、逐文件状态、目录哈希、数据分组泄漏、事件恢复和网格中断收敛歧义均已修正；定向复核最终通过。

## 2026-07-19 重新复核

- 用户确认 SQLite-only 网格记录后，设计稿、执行清单和产物契约已统一删除默认网格 JSON sidecar。
- 人工真值、TIF/Local Axis、推理基线和 TIF 导出定向回归：278/278 通过。
- headless 训练入口与人工真值门禁回归：15/15 通过。
- 产物契约中的 8 个 JSON 示例全部重新解析通过；这些示例只覆盖获准存在的训练、完整性、推理记录或 SQLite 逻辑投影，不构成网格 sidecar。
- `git diff --check` 通过后，第一阶段门禁重新判定为通过，可以进入第二阶段。

## 第二阶段继续处理

1. 让 headless 项目加载和预检失败也生成统一失败记录，不能只退出进程。
2. 阻止 `external_blink` 配置静默落入 ViT-B 训练分支，实际后端必须与记录一致。
3. TIF-Blink preprocessed CLI 必须增加外部可信来源、完整性清单和实际 train/validation split 门禁。
4. 实现本阶段冻结的完整性、训练运行和异常善后契约；当前文档不代表这些功能已经上线。

## 产物

- `docs/contracts/taxamask_round5_training_entry_matrix.md`
- `docs/contracts/taxamask_round5_artifact_contract_v1.md`
- `tests/test_predict_full_pipeline_baseline.py`
- `tests/baselines/predict_full_pipeline_v1.json`
